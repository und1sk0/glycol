apiVersion: v1
kind: Secret
metadata:
  name: glycol-credentials
  namespace: glycol
type: Opaque
stringData:
  # Replace with your actual OpenSky Network credentials
  credentials.json: |
    {
      "client_id": "your-opensky-client-id",
      "client_secret": "your-opensky-client-secret"
    }
